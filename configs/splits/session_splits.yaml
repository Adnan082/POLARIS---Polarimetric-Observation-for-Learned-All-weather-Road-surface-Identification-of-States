# PRISM train/val split is defined by the dataset authors (train/ and val/ dirs in the repo)
# Some sessions appear in BOTH train and val (intra-session splits for rare classes)
# Do NOT override this split — use it as-is via PRISMDataset(split="train"/"val")
#
# Session names decoded from the data card (anonymised, date-indexed):
# Format: {date_index}_{seq_index}_dataset
# Full mapping released at camera-ready stage via dataset_name_remapping.json

# Known sessions (from HuggingFace file listing):
# 0106_dataset, 0112_dataset, 0119_1_dataset, 0119_2_dataset, 0120_dataset,
# 0124_dataset, 0129_1_dataset, 0129_2_dataset, 0129_3_dataset, 0129_4_dataset,
# 0202_dataset, 0210_1_dataset, 0210_2_dataset, 0318_dataset, 0327_dataset, ...
# (41 sessions total)

# For domain-transfer experiment (proving_ground → open_road):
# proving_ground sessions are identifiable by road_type="proving_ground" in labels.json
# Extract them programmatically from labels rather than hardcoding here.
